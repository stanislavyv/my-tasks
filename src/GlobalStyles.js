import GlobalStyles from '@mui/material/GlobalStyles';

const globalStyles = (
    <GlobalStyles
        styles={{
            html: {
                fontFamily: 'Roboto',
            },

            body: {
                margin: 0,
                padding: 0,
            },
        }}
    />
);

export default globalStyles;
